# Memory Index

- [React Three Fiber Canvas needs a WebGL guard](r3f-webgl-fallback.md) — uncaught WebGL context-creation failures crash the whole app; always wrap `<Canvas>` in its own error boundary + feature-detect fallback.
